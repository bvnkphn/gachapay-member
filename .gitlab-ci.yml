workflow:
  rules:
    # Prevents duplicate pipelines when a merge request is open
    - if: '$CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS && $CI_PIPELINE_SOURCE == "push"'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'

variables:
  SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"
  GIT_DEPTH: "0"
  NODE_VERSION: "24.17"
  IMAGE_NAME: "${CI_REGISTRY_IMAGE}:${CI_COMMIT_SHORT_SHA}"
  IMAGE_LATEST: "${CI_REGISTRY_IMAGE}:latest"

stages:
  - test
  - build
  - sast
  - container-scan
  # - dast

# ─── Unit Test + Coverage ────────────────────────
# test:
#   stage: test
#   image: node:${NODE_VERSION}-alpine
#   cache:
#     key:
#       files:
#         - package-lock.json
#     paths:
#       - node_modules/
#       - .next/cache/
#   before_script:
#     - npm ci --prefer-offline
#   script:
#     - npm run test -- --coverage --coverageReporters=text --coverageReporters=cobertura --coverageReporters=html
#   coverage: '/Lines\s*:\s*(\d+\.?\d*)%/'
#   artifacts:
#     when: always
#     reports:
#       coverage_report:
#         coverage_format: cobertura
#         path: coverage/cobertura-coverage.xml
#       junit: junit.xml
#     paths:
#       - coverage/
#   rules:
#     - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
#     - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'

# ─── ESLint ──────────────────────────────────────
lint:
  stage: test
  image: node:${NODE_VERSION}-alpine
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules/
  before_script:
    - npm ci --prefer-offline
  script:
    - npm run lint
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'

# ─── TypeScript Type Check ────────────────────────
typecheck:
  stage: test
  image: node:${NODE_VERSION}-alpine
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules/
  before_script:
    - npm ci --prefer-offline
  script:
    - npx tsc --noEmit
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'

# ─── Build Docker Image ──────────────────────────
build:
  stage: build
  image: docker:26-cli
  services:
    - docker:26-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  script:
    - docker build
        --cache-from "$IMAGE_LATEST"
        --build-arg NODE_VERSION=${NODE_VERSION}
        --build-arg NEXT_PUBLIC_APP_ENV=${CI_ENVIRONMENT_NAME:-production}
        --label "org.opencontainers.image.revision=${CI_COMMIT_SHA}"
        --label "org.opencontainers.image.created=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
        -t "$IMAGE_NAME"
        -t "$IMAGE_LATEST"
        .
    - docker push "$IMAGE_NAME"
    - docker push "$IMAGE_LATEST"
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'

# ─── SonarCloud ───────────────────────────────────────────
sonarcloud-check:
  stage: sast
  image:
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script:
    - >
      if [ "$CI_PIPELINE_SOURCE" == "merge_request_event" ]; then
        sonar-scanner \
          -Dsonar.pullrequest.key=$CI_MERGE_REQUEST_IID \
          -Dsonar.pullrequest.branch=$CI_MERGE_REQUEST_SOURCE_BRANCH_NAME \
          -Dsonar.pullrequest.base=$CI_MERGE_REQUEST_TARGET_BRANCH_NAME
      else
        sonar-scanner -Dsonar.branch.name=$CI_COMMIT_REF_NAME
      fi
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'

# ─── Semgrep ──────────────────────────────────────────────
semgrep:
  stage: sast
  image: semgrep/semgrep:latest
  variables:
    SEMGREP_APP_TOKEN: $SEMGREP_APP_TOKEN
  script:
    - semgrep ci
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'
  allow_failure: true

# ─── Trivy (filesystem) ───────────────────────────────────
trivy-fs-scan:
  stage: container-scan
  image:
    name: aquasec/trivy:latest
    entrypoint: [""]
  variables:
    TRIVY_NO_PROGRESS: "true"
    TRIVY_CACHE_DIR: ".trivycache"
  cache:
    key: trivy-cache
    paths:
      - .trivycache/
  script:
    - trivy fs
        --exit-code 0
        --severity MEDIUM,HIGH,CRITICAL
        --scanners vuln,secret,misconfig
        --format template
        --template "@/contrib/html.tpl"
        --output trivy-fs-report.html
        .
  artifacts:
    when: always
    paths:
      - trivy-fs-report.html
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'
  allow_failure: true

# ─── Trivy (image) ────────────────────────────────────────
trivy-image-scan:
  stage: container-scan
  needs: ["build"]
  image:
    name: aquasec/trivy:latest
    entrypoint: [""]
  variables:
    TRIVY_NO_PROGRESS: "true"
    TRIVY_CACHE_DIR: ".trivycache"
    TRIVY_USERNAME: "$CI_REGISTRY_USER"
    TRIVY_PASSWORD: "$CI_REGISTRY_PASSWORD"
  cache:
    key: trivy-cache
    paths:
      - .trivycache/
  script:
    - trivy image
        --exit-code 0
        --severity MEDIUM,HIGH,CRITICAL
        --format template
        --template "@/contrib/html.tpl"
        --output trivy-image-report.html
        "$IMAGE_NAME"
  artifacts:
    when: always
    paths:
      - trivy-image-report.html
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'
  allow_failure: true

# ─── ZAP (DAST) ───────────────────────────────────────────
# zaproxy:
#   stage: dast
#   image:
#     name: ghcr.io/zaproxy/zaproxy:stable
#     entrypoint: [""]
#   variables:
#     ZAP_TARGET_URL: "${TARGET_URL}"
#   script:
#     - mkdir -p /zap/wrk
#     - zap-baseline.py
#         -t "$ZAP_TARGET_URL"
#         -r zap-report.html
#         -I
#   artifacts:
#     when: always
#     paths:
#       - zap-report.html
#   rules:
#     - if: '$CI_COMMIT_BRANCH == "main" || $CI_COMMIT_BRANCH == "develop"'
#   allow_failure: true